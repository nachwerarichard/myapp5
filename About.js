
    function about(){
        return(
    <body>
    <h1>About Us</h1>
    <p>You can control the values of more than one input field by adding a name attribute to each element.

    We will initialize our state with an empty object.
    
    To access the fields in the event handler use the event.target.name and event.target.value syntax.
    
    To update the state, use square brackets bracket notationaround the property n</p>
    </body>
    );
    };
    export default about;